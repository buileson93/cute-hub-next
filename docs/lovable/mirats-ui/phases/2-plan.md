# Phase 2: Button & IconButton Parity (Plan)

## Goal
Implement exact visual parity for buttons using static Astryx CSS.

## Steps
1. Update `src/components/ui/button.tsx` with data-astryx attributes.
2. Update `src/styles/astryx-static/components.css` with precise skinning and geometry rules.
3. Pilot verification in Dashboard and Model Catalog.

## Verification
- Computed styles (32px height, 13px text).
- MIRATS Blue consistency.
- Scale effect on active state.
