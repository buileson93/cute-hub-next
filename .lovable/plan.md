# Visual Text Update: System Execution Contract

Update the User Menu in the AppShell to reflect the "SYSTEM EXECUTION CONTRACT" for future development standards.

## Changes

### src/components/mirats/app-shell/index.tsx
- Replace the "Unified Anatomy & Responsive Frame" manifesto content with the "SYSTEM EXECUTION CONTRACT" details in the `isInventoryMode` block.

## Technical Details
- The contract specifies strict implementation guidelines: no prompt leakage into UI, real source diffs, no mock UI, and mandatory visual verification.
- Maintain the `whitespace-pre-wrap` and `font-mono` styling for developer-centric readability.
- This is a direct implementation to confirm the execution contract as requested.

