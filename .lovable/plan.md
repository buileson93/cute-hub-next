# Plan - Update TzClock aria-label

The user wants to update the `aria-label` of the `TzClock` component to a specific Vietnamese string.

## User Review Required

> [!IMPORTANT]
> This change is purely visual/metadata (aria-label) as requested.

## Technical Details

### Frontend Changes

- Update `src/components/mirats/TzClock.tsx`:
    - Change `aria-label` to `"thêm sửa cột tài sản lắp có thây đổi chiều rộng cột"`.

